# ─── Core web framework ──────────────────────────────────────────────────────
flask>=2.3.0
flask-socketio>=5.3.0
python-engineio>=4.8.0
python-socketio>=5.10.0

# ─── Computer vision ─────────────────────────────────────────────────────────
opencv-python>=4.8.0          # SSD face detector, solvePnP (EPnP), image I/O
numpy>=1.24.0                 # Array operations for landmark geometry

# ─── Face recognition (dlib ResNet-128 embeddings) ───────────────────────────
# Requires cmake + dlib C++ build.
# On Ubuntu/Debian: sudo apt install cmake libdlib-dev
# On macOS:        brew install cmake dlib
# On Windows:      pip install cmake dlib  (Visual Studio Build Tools required)
face-recognition>=1.3.0       # wraps dlib's face_recognition_models

# ─── MediaPipe (gaze/pose tracking) ──────────────────────────────────────────
mediapipe>=0.10.0             # Face Mesh (468 landmarks), real-time CPU inference

# ─── Utility ─────────────────────────────────────────────────────────────────
Pillow>=10.0.0                # Image utilities
python-dotenv>=1.0.0          # Environment variable loading

# ─── Audio monitoring (AudioAgent) ──────────────────────────────────────────
# WebRTC VAD: Google's GMM-based Voice Activity Detector (~5ms/frame on CPU).
# Uses pre-built wheels — no C compiler needed on Windows/macOS/Linux.
webrtcvad-wheels>=2.0.10      # WebRTC VAD — pre-built for all platforms

# ─── Development (optional) ──────────────────────────────────────────────────
# eventlet>=0.33.0            # Production async mode for SocketIO
# gevent>=22.0.0              # Alternative async mode
